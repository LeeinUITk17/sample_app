<%= form_with(
    model: current_user.active_relationships.find_by(followed_id: user.id),
    method: :delete
    ) do |f| %>
  <%= f.submit t(".unfollow"), class: "btn", data: { disable_with: t(".unfollowing") } %>
<% end %>
