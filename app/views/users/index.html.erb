<% provide(:title, t(".title")) %>
<h1><%= t(".title") %></h1>

<%= link_to t(".new_user"), new_user_path, class: "btn btn-primary" %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
